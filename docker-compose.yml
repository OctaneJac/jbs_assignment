# Docker file not working

services: 
  # db:
  #   image: postgres:latest
  #   environment: 
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: my_db
  #   volumes: 
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

  backend:
    build: ./backend
    ports: 
      - "8000:8000"
    environment: 
      - DATABASE_URL=postgresql://neondb_owner:npg_erldOquG3c7b@ep-sparkling-brook-a1avjedb-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=verify-full&channel_binding=require

  frontend:
    build: 
      context: ./frontend
      args: 
        NEXT_PUBLIC_API_URL: http://localhost:8000
    ports: 
      - "3000:3000"
    environment: 
      - BETTER_AUTH_SECRET=2xo8bumKjzjxXCx6MauXwqQhVNN4CziJ
      - DATABASE_URL=postgresql://neondb_owner:npg_erldOquG3c7b@ep-sparkling-brook-a1avjedb-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=verify-full&channel_binding=require
    depends_on:
      - backend

# volumes:
#   postgres_data:  